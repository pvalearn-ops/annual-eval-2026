# 115年度代行檢查機構年終考評 · 3D 互動展示網頁

一個工業控制室風格、帶科技/AI 氛圍的 3D 互動網站，可直接部署到 GitHub Pages，並針對手機觀看優化。

- 進場為「工業齒輪核心」3D 場景（旋轉齒輪機組、爐心發光、危險警戒環、藍圖網格、火花粒子，可拖曳旋轉）。
- 三大互動節點，造型貼合危險性機械設備：
  - **代檢業務報告** → 壓力容器造型（條列說明）
  - **優良事蹟** → 六角螺帽造型（圖片相簿）
  - **創新作為** → 齒輪造型（AI 主題特效）
- 點擊任一設備物件即展開對應內容面板。

---

## 1. 檔案結構

```
115年考評網頁/
├─ index.html            ← 網頁主檔（不用改）
├─ css/style.css         ← 樣式（不用改）
├─ js/
│  ├─ data.js            ← ★ 你要改的內容都在這裡（文字、數據、圖片、卡片）
│  └─ main.js            ← 3D 引擎（一般不用改）
├─ assets/
│  ├─ images/            ← 放「優良事蹟」的照片
│  └─ models/            ← 放自訂 3D 模型（.glb）
└─ README.md
```

---

## 1-1. 115 年度資料與附件（2026 版更新）

- 內容來源：`115年資料/代檢業務報告.doc`（115年度工作績效自我評鑑表，期間 114/7～115/6）與 `115年資料/優良事項.docx`。
- 網頁資料已同步更新於 `js/data.js`：
  - **代檢業務報告**：六大項，點「展開詳情」後依原評鑑表的小項分組（共 28 個小項），每則說明前面都有題號（例如 `2-3`＝第 2 小項第 3 點），方便考評委員對應原文件。
  - 原文件內的 22 張統計表放在各自的小項底下，平常收合成一列標題（例如「表 1-1 量測儀器校驗統計」），點一下才以動畫展開（表格淡入上浮、資料列逐列滑入、表頭光澤掃過）；再點一次收合。
  - 附件／附表 PDF 連結依原評鑑表「備註說明」欄，掛在各自對應的小項底下（例如附表二之7 與附件五放在第 2 大項第 7 小項）。
  - **重點標示**：內文的數據（數字＋座／件／場次／家／％／元等單位）會自動以紅字＋黃色螢光標示；若想自行強調其他字句，在 `js/data.js` 的文字裡用 `**要強調的字**` 包起來即可（標題、表格、附件名稱不受影響）。
  - **優良事項**：依 `優良事項.docx` 原文照登（五大面向、[1]～[31] 共 31 項），文字未再改寫。
  - **創新作為**：維持原 115 年度 AI 創新內容不變。
- 附件 PDF 一律放在 `assets/att115/`，命名規則：
  - `a01`～`a10`（含 `a06-1`、`a08-1`）＝ 附件一～附件十
  - `t1-1`、`t2-*`、`t3-*`、`t6-*` ＝ 附表一之1～附表六之6
  - `t6-6-p1-*` / `t6-6-p2-*` ＝ 前次考評缺失改善佐證
  - `src-report.pdf` / `src-merit.pdf` ＝ 原始文件 PDF
- 原 Word/Excel 附件已轉成 PDF，瀏覽器可直接開啟；原始檔仍保留在 `115年資料/`。
- 114 年度舊版資料備份於 `js/data.js.bak114`。

### ⚠️ 3D 模型要用「啟動網頁.bat」開啟（重要）

`index-3d.html` 會載入 3 個模型：

| 模型檔 | 用途 |
|---|---|
| `assets/models/boiler.glb` | 代檢業務報告節點 |
| `assets/models/crane.glb` | 優良事項節點 |
| `assets/models/factory.glb` | 場景中的工廠建築 |

（`factory2.glb`、`boiler_test.glb` 目前沒有使用，是備用檔。）

**用滑鼠雙擊 `index-3d.html`（file:// 開啟）時模型不會載入**，因為瀏覽器的 CORS 政策禁止 file:// 網頁以 XHR/fetch 讀取本機檔案，GLTFLoader 會失敗，畫面就退回內建的幾何造型（藍色桶子、獎盃、塔吊）。文字、表格、附件都不受影響，只有 `.glb` 讀不到。

解決方式擇一：

1. **雙擊資料夾內的 `啟動網頁.bat`**（最簡單）：會自動用 Python 或 Node.js 起一個本機伺服器並開啟 `http://localhost:8000/index-3d.html`，看完關掉黑色視窗即可。
2. 自行開伺服器：`py -3 -m http.server 8000` 或 `npx serve .`，再用 http 網址開啟。
3. 部署到 GitHub Pages 等網站空間，用網址開啟。

以 file:// 開啟時，畫面右下角會出現黃色提示條說明原因。

若要讓「創新作為」節點也換成自訂模型，在 `js/data.js` 的該段加一行 `model: 'assets/models/你的檔案.glb',` 即可（程式已支援）。

### 3D 版（index-3d.html）的閱讀面板寬度

- 手機（寬度 < 820px）：維持原本的由下往上彈出、滿版單欄，未做任何更動。
- 桌機（寬度 ≥ 820px）：面板寬度改為隨螢幕縮放 `clamp(460px, 46vw, 900px)`，1600px 以上再放寬到 `clamp(560px, 44vw, 1040px)`；卡片改為自動排列（面板越寬欄數越多），KPI 數據卡同理。
- 面板右上角新增「⤢ 放大閱讀」按鈕，可把面板撐到 `min(1280px, 94vw)`，寬表格就不用左右捲動；再按一次（⤡）還原。此設定會記在瀏覽器裡，下次開啟沿用。

---

## 2. 如何修改內容（不用懂程式）

打開 **`js/data.js`**，用記事本或 VS Code 編輯，存檔後重新整理網頁即可。

- **改標題**：最上面 `SITE.title`。
- **代檢業務報告**：改 `stats`（數據卡）與 `items`（條列，每筆有 `heading` 標題和 `text` 內文）。
- **優良事蹟**：改 `gallery`，每筆有 `img`（照片檔名）、`title`、`caption`。
- **創新作為**：改 `typewriter`（開場打字句）與 `cards`（每張卡有 `icon` emoji、`title`、`desc`）。

> 提示：中文引號要用英文的 `'` 或 `"`，每筆資料結尾記得留逗號 `,`。

---

## 3. 放照片（優良事蹟）

1. 把照片放進 `assets/images/`，例如 `merit-1.jpg`。
2. 確認 `js/data.js` 裡 gallery 的 `img` 檔名一致。
3. 建議寬約 1200px、單張 300KB 以內，手機載入較快。

照片還沒放時會自動顯示漸層佔位圖，之後替換即可。

---

## 4. 提交 3D 模型給網頁呈現 ★（你問的重點）

網頁支援載入你自己的 3D 模型，取代預設的幾何造型。

### 你要提供的格式
- **檔案格式：`.glb`**（glTF 二進位，單一檔案含材質，最通用）。
- 面數建議 **5 萬面以內**（手機也順）；貼圖尺寸 1024～2048px。
- 模型請置中、比例正常，網頁會自動縮放到合適大小。

### 怎麼做出 .glb？
- **用 Blender（免費）**：做好模型後 `檔案 → 匯出 → glTF 2.0 (.glb)`。
- **用線上工具**：把 obj/fbx 丟到 https://products.aspose.app/3d/conversion 之類轉成 glb。
- **免費模型庫**：Sketchfab、Poly Pizza 可下載可商用的 glb。

### 怎麼交給我 / 怎麼啟用
把 `.glb` 檔放進 `assets/models/`，然後打開 `js/data.js`，把對應項目那行的 `//` 拿掉：
```js
model: 'assets/models/report.glb',
```
重新整理網頁，該節點就會換成你的模型（會自動緩慢旋轉）。

> 如果你把 .glb 檔傳給我，我可以直接幫你接好、調整大小與旋轉，你只要看成果。

---

## 5. 部署到 GitHub Pages

1. 在 GitHub 建立一個新的 repository（例如 `annual-eval-2026`）。
2. 把本資料夾所有檔案上傳（可用網頁拖曳上傳，或 git push）。
3. 進入 repo 的 **Settings → Pages**。
4. Source 選 **Deploy from a branch**，Branch 選 **main / (root)**，按 Save。
5. 等 1～2 分鐘，Pages 會給你一個網址（`https://你的帳號.github.io/repository名稱/`），手機打開即可。

用 git 上傳的指令範例：
```bash
git init
git add .
git commit -m "115年度代行檢查考評展示網頁"
git branch -M main
git remote add origin https://github.com/你的帳號/annual-eval-2026.git
git push -u origin main
```

---

## 6. 本機預覽

**直接用滑鼠雙擊 `index.html` 就能開**（用 Chrome / Edge 開啟即可，需連網載入 3D 函式庫）。

若日後想改回新版模組載入或架站測試，也可用簡易伺服器：
```bash
npx serve .
```
然後瀏覽器開 `http://localhost:3000`。

---

## 備註
- 3D 函式庫（Three.js）與字型從 CDN 載入，觀看時需連網。
- 若要完全離線，可再告訴我，我幫你把函式庫改成本機引入。
